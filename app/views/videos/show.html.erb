
  <div class="container" style="margin-top:20px;" >
  <nav class="navbar bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path %>">
     <img src="<%= asset_url('youtube.png') %>" alt="Logo" width="45" height=40"" class="d-inline-block align-text-top">
     UTube
    </a>
  </div>
</nav>
<div class="text-success">
  <hr>
</div>
<div class="container">
<div class="card text-center" style="margin-top:20px;">
  <div class="card-body">
  <iframe width="1000" height="500" src="<%=@video.link %>" frameborder="0" allowfullscreen="false"></iframe>    
  <p class="card-text">
  <%@video.tags.each do |t| %>
  <%= link_to t.name %>

  <%end%>
  </p>
  <p class="card-text"><%=@video.title%></p>
  <p class="card-text"><%=@video.description%></p>

  </div>
  <div class="card-footer text-muted">
    Uploaded On: <%= @video.created_at %>
  </div>
</div>

</div>

</div>